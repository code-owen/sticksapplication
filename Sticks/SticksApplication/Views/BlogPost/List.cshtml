@model IEnumerable<SticksApplication.Models.BlogPostDto>
@{
    ViewBag.Title = "List";
}

<div class="bg-secondary bg-opacity-10 py-2 mb-5">
    <div class="container">
        <h1>Blog Posts</h1>
    </div>
</div>

<partial name="_Notification">

    @if (Model != null && Model.Any())
    {
        <div class="container">
            <table class="table">
                <thead>
                    <tr>
                        <td><strong>BlogPostId</strong></td>
                        <td><strong>Heading</strong></td>
                        <td> </td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var blogPost in Model)
                    {
                        <tr>
                            <td>@blogPost.BlogPostId</td>
                            <td>@blogPost.Heading</td>
                            <td><a href="/BlogPost/Details/@blogPost.BlogPostId">View</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="container">
            <p>No blog posts were found!</p>
        </div>
    }
